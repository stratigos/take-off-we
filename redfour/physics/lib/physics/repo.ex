defmodule Physics.Repo do

  # `use` The Ecto module to pull its macros into this module!
  # Then `mix ecto.gen.migration add_some_table` with Mix to get started.
  use Ecto.Repo, otp_app: :physics


end
